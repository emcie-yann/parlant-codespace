[project]
name = "parlant-codespace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # Install Parlant directly from Git (develop branch)
    "parlant @ git+https://github.com/emcie-co/parlant.git@develop"
]
